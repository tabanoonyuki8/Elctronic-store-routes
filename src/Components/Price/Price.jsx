import PropTypes from 'prop-types';

const Price = ({option}) => {
    return (
        <div>
            
        </div>
    );
};
Price.propTypes={
    option:PropTypes.object.isRequired
  
  }
export default Price;